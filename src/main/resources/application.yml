com:
  example:
    properties:
      value: "application"

spring:
  profiles:
    group:
      default:
        - common
        - dev
      staging:
        - common
        - staging
      prod:
        - common
        - prod
